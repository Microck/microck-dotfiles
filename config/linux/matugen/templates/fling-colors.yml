colors:
  background: "{{ colors.surface.default.hex }}"
  foreground: "{{ colors.on_surface.default.hex }}"
  accent: "{{ colors.primary.default.hex }}"
  match: "{{ colors.secondary.default.hex }}"
  selection: "{{ colors.surface_container_low.default.hex }}"
